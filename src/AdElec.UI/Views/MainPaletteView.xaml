<UserControl x:Class="AdElec.UI.Views.MainPaletteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Background="#1E1E1E" Padding="10">
            <TextBlock Text="AD-ELEC / Panel de Control" 
                       Foreground="White" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"/>
        </Border>

        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="Tableros" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox x:Name="CmbPanels" Margin="0,0,0,10"/>
            <Button Content="Nuevo Tablero" Padding="5" Margin="0,0,0,10"/>
            <Button Content="Sugerir Luminarias" Padding="5" Margin="0,0,0,10"/>
            <Button Content="Sugerir Tomas" Padding="5" Margin="0,0,0,10"/>
            <Button Content="Generar Unifilar" Padding="5"/>
        </StackPanel>
    </Grid>
</UserControl>
